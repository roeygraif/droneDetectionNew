"""collapseprobe — localizing where a deep low-SNR detector loses the target.

See PROPOSAL.md for the thesis-direction writeup. The synthetic dataset is built
by ``collapseprobe.dataset`` and reuses the validated ``src/synthetic`` +
``src/tbd`` pipeline (the physics is shared with ``npceiling`` on purpose).
"""
